{{#partial "usuario"}}
    <h6>{{usuario}}</h6>
{{/partial}}

{{#partial "content"}}
    <h2>Ingresar Medición</h2>
    <form class="flex-clm form-evo-hc" id="acciones">
        <label for="tipo-consumo"><h3>Tipo de consumo</h3></label>
        <select id="tipo-consumo" name="tipo-consumo" class="flex-clm form form-evo-hc">
            <option disabled="">Selecciona una opcion</option>
        {{#each tipoconsumos}}
                <option value="{{nombre}}"> {{nombre}}</option>
            {{/each}}
        </select>

        <label for="periodicidad" ><h3>Periodicidad</h3></label>
        <select id=periodicidad name="periodicidad" class="flex-clm form form-evo-hc">
            <option disabled="">Selecciona una opcion</option>
            <option value="Anual">Anual</option>
            <option value="Mensual">Mensual</option>
        </select>

        <label for="valor"><h3>Valor</h3></label>
        <input type="number" min="1" class="flex-clm form form-evo-hc" id="valor" name="valor">

        <label for="fecha-medicion"><h3>Fecha de medicion</h3> </label>
        <input max="2023-01" type="month" id="fecha-medicion"
               name="fecha-medicion" class="flex-clm form form-evo-hc">
        <div>
            <button type="submit" form="acciones" formaction="/home/mediciones"
                    class="btn-2 first">
                 Atrás
            </button>
            <button id="ingresar" type="submit" form="acciones" formmethod="post"
                    formaction="/home/mediciones/perse/creado"
                    class="btn-consulta destacar">
                Guardar
            </button>
        </div>

        {{#if tipoConsumoNull}}
            <div class="btn-error firstError">El tipo de consumo ingresado no existe</div>
        {{/if}}
        {{#if camposIncompletos}}
            <div class="btn-error firstError">Alguno de los campos está incompleto</div>
        {{/if}}
        {{#if exito}}
            <div class="btn-success firstSuccess">La medicion fue cargada con exito</div>
        {{/if}}

    </form>



{{/partial}}

{{>organizacion-home-iniciado-sesion.hbs}}